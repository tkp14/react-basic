import {Counter, ToggleButton, TodoList, Count} from "./components";

function App() {
  return (
    <div className="container">
      <Counter />
      <ToggleButton />
      <TodoList />
      <Count />
    </div>
  );
}

export default App;
